import React from 'react';

const TodoList = () => {
    return (
        <div className="todo-container">
            <ul className="todo-list">Todo List says hello.</ul>
        </div>
    )
};

export default TodoList;